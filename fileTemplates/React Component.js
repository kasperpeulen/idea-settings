import React from 'react';

class ${NAME} extends React.Component {

    constructor(attributes) {
        
        super(attributes);
    }

    render() {
    
        return (
            <div>${NAME}</div>
        );
    }
}

export default ${NAME};